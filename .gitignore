# ── Data tiers (mostly large/licensed — do not commit) ────────────────────
data/raw/
data/interim/
data/external/*.tif
data/external/*.h5
data/external/*.nc
# Census TIGER Line (county zip + extract; large — cache locally)
data/external/tiger/
data/processed/

# ── Application cache (aiohttp, HTTP, etc.) ──────────────────────────────
cache/

# ── Python cache ──────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python

# ── Virtual environments ──────────────────────────────────────────────────
.venv/
venv/
env/
ENV/

# ── Local AI / editor tooling (do not commit) ─────────────────────────────
.claude/

# ── Internal project context (not for submission) ─────────────────────────
context/
DECISIONS.md
ASSUMPTIONS.md

# ── Jupyter ───────────────────────────────────────────────────────────────
.ipynb_checkpoints/
*.ipynb_checkpoints

# ── Secrets / credentials ─────────────────────────────────────────────────
.env
*.key
*.pem
secrets/

# ── Large artifact files (keep logs and small outputs) ────────────────────
artifacts/models/*.pkl
artifacts/models/*.joblib
artifacts/models/*.pt
artifacts/models/*.h5
artifacts/logs/runs/*/

# ── OS / editor noise ─────────────────────────────────────────────────────
.DS_Store
._*
**/._*
Thumbs.db
*.swp
.idea/
.vscode/

# ── Build ─────────────────────────────────────────────────────────────────
dist/
build/
*.egg-info/

# Local workflow / brainstorming tooling
.superpowers/
dashboard/dev-agentic-workflow-kit/
